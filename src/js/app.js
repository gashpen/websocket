import AppController from "./Components/AppController";

const container = document.querySelector('.container');

const appController = new AppController(container);

appController.addLoginWindow();

